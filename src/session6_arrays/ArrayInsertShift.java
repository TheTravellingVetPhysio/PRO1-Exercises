/** package session6_arrays;

import java.util.Arrays;

public class ArrayInsertShift
{
  public static void main(String[] args)
  {
    int[] n = {5, 10, 15, 20, 25, 0, 0};
    int newN = 30;
    int position = 2;

    insertAtPosition(n, newN, position);
    System.out.println("Updated array: " + Arrays.toString(n));
  }

  /** public static void insertAtPosition(int[] array, int value, int index)
  {
    n[2] = 30;
    String formatArray = Arrays.toString(n);
    System.out.println(formatArray);
  } */

  /**public static void insertAtPosition(int[] array, int value, int index)
  {
    do (insertAtPosition();)
  } */